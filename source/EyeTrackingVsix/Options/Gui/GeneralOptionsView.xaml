<UserControl x:Class="EyeTrackingVsix.Options.Gui.GeneralOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EyeTrackingVsix.Options.Gui"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:GeneralOptionsViewModel, IsDesignTimeCreatable=False}"
             d:DesignWidth="500" d:DesignHeight="700">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <local:CaretOptionsView DataContext="{Binding Caret}" />
            <local:WindowFocusOptionsView DataContext="{Binding WindowFocus}" />
            <local:ScrollOptionsView DataContext="{Binding Scroll}" />
            <local:KeyboardOptionsView />
        </StackPanel>
    </ScrollViewer>
</UserControl>
